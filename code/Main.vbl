from math import *
from io import *

function main()
    i = 0
    while i < 5
        $(i,0) = i
        i = i + 1
    end

    i = 0
    while i < 5
        $(i,1) = $(i,0) + 1
        --io::print("A: " + math::getInt(a) + "\n")
        i = i + 1
    end

    return
end